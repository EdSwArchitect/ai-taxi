# Use DockerContextClientProviderStrategy for Docker Desktop
docker.client.strategy=org.testcontainers.dockerclient.DockerContextClientProviderStrategy
# Set Docker API version to match Docker Desktop (1.52)
docker.api.version=1.52
ryuk.disabled=true
checks.disable=true
